# ——— Server ————————————————————————————————————————
PORT=8080
NODE_ENV=development

# ——— Firebase Admin ————————————————————————————————
# Percorso assoluto al service account JSON
GOOGLE_APPLICATION_CREDENTIALS=/workspaces/dev/backend/sa.json

# ——— Autorizzazioni Admin ————————————————————————————
# Email (o elenco separato da virgole) autorizzate a fare POST/IMPORT
ADMIN_EMAILS=schiavone_lu@hotmail.it

# ——— Config lega ————————————————————————————————
# Slug usato da API e cron per leggere/scrivere i dati
LEAGUE_SLUG=dbcarognaleague

# ——— URL pubblico backend (utile per link/callback) ——————
PUBLIC_URL=http://localhost:8080

# ——— Debug / sviluppo ——————————————————————————————
# Abilita endpoint /api/_debug/* (seed ecc.)
ALLOW_DEBUG_SEED=1
# Consenti import “liberi” (JSON/CSV senza sheet) in dev
ALLOW_INSECURE_IMPORT=1

# ——— Cron import classifica da Google Sheet ———————————
# Abilita il cron (0=off, 1=on)
CRON_ENABLED=1
# Esecuzione all’avvio (metti 0 per non avere log “skip” se non hai lo sheet)
CRON_RUN_ON_BOOT=0
# Orario cron (lun e mar alle 08:00, orario di Roma)
CRON_STANDINGS_SCHEDULE=0 8 * * 1,2
# Fuso orario
TZ=Europe/Rome

# (OPZIONALE) Quando avrai il foglio pubblicato in CSV, abilita le due righe sotto:
# STANDINGS_SHEET_CSV_URL=https://docs.google.com/spreadsheets/d/.../pub?output=csv
# STANDINGS_DELIMITER=,






